apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: azure-resources-repo
  namespace: cluster-config
spec:
  interval: 30s
  url: https://github.com/underguiz/aks-gitops
  ref:
    branch: main